<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Desert Pulse</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="auth-container fade-in">
    <div class="auth-card">
      <div class="auth-title">
        <h1 style="font-size: 2.5rem;">DESERT PULSE</h1>
        <p style="color: var(--sand-medium); margin-top: 1rem;">Return to Your Journey</p>
      </div>

      <% if (error) { %>
        <div class="alert alert-error">
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/login">
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required>
        </div>

        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required>
        </div>

        <button type="submit" class="btn" style="width: 100%; margin-top: 1rem;">LOGIN</button>
      </form>

      <div class="auth-link">
        <p>New to Desert Pulse? <a href="/signup">Create Account</a></p>
      </div>

      <div class="auth-link" style="margin-top: 2rem;">
        <a href="/" style="color: var(--sand-medium);">← Back to Home</a>
      </div>
    </div>
  </div>
</body>
</html>
